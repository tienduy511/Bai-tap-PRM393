# pubspec.yaml
# Exercise 12.2 — Chỉ khai báo asset thực sự tồn tại và được dùng
# Exercise 12.3 — Giữ dependency tối thiểu để giảm kích thước APK

name: lab12 
description: "Ứng dụng quản lý task — Lab 12 Performance Optimization"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # provider: state management nhẹ, không kéo theo dependency nặng
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    # Exercise 12.2 — Chỉ khai báo asset thực sự dùng
    # Xóa bất kỳ asset nào không được tham chiếu trong code
    - assets/icon_128.png

  # Không khai báo font thừa để giảm kích thước APK
